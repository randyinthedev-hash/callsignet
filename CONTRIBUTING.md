# 기여 안내

Callsignet을 고치거나 늘리려는 사람을 위한 문서다. 써 보려는 사람은 [README](README.md)를 보면 된다.

## 설계를 먼저 본다

기능을 더하기 전에 [design/README.md](design/README.md)를 읽는다. 무엇을 왜 그렇게 정했는지가 거기 있다. 그 문서가 정한 것과 어긋나는 코드는 받지 않는다. 설계를 바꿔야 한다고 보면 코드보다 이슈가 먼저다.

무엇을 어느 계층에서 확인하는지는 [TESTING.md](TESTING.md)에 있다. 시험을 새로 쓸 때 그 문서를 먼저 본다.

## 사전 요구

Go 1.26 이상이 필요하다. csa는 리눅스에서만 돈다. TUN 인터페이스를 만들고 경로를 넣으므로 네트워크 관리 권한이 필요하고, 직통 경로를 닫는 데 `nft` 명령을 쓰므로 nftables도 있어야 한다.

### 어디서 만들 수 있나

| 운영체제 | `make build` | 단위 시험 | 통합 시험 |
|---|---|---|---|
| 리눅스 | 된다 | 전부 돈다 | 된다. root가 필요하다 |
| macOS | 안 된다 | `internal/wgdev`을 뺀 나머지가 돈다 | 안 된다 |
| Windows | 안 된다 | 확인하지 않았다 | 안 된다 |

csa는 커널과 닿는 부분을 리눅스에서만 만든다. 그래서 다른 운영체제에서는 `cmd/csa`가 빌드되지 않는다. `make test`는 `go vet ./...`을 먼저 돌리므로 macOS에서 그 자리에서 실패한다. macOS에서 순수한 판단만 고쳤다면 꾸러미를 골라 시험을 돌린다.

```bash
go test ./internal/config/ ./internal/policy/ ./internal/name/ ./internal/guard/ ./internal/control/
```

커널과 닿는 것을 고쳤다면 리눅스에서 확인한다. 통합 시험은 흉내가 아니라 csa를 실제로 돌리는 것이라 리눅스가 있어야 한다.

## 개발 루프

```bash
make build     # csa를 만든다
make test      # go vet과 단위 시험을 돌린다
sudo make tunnel   # 네임스페이스 셋에 csa 둘을 실제로 띄워 확인한다
```

네임스페이스에 없는 것은 VM에서 본다. libvirt가 필요하다.

```bash
make build-static
sudo make vm
```

**통합 시험을 돌리기 전에 무엇을 고치는지 안다.** 시험 스크립트는 그 머신의 네임스페이스와 브리지를 실제로 만들고 리졸버 설정을 실제로 고친다. 끝날 때 스스로 지우고 되돌리며, 실패해도 되돌린다. 그래도 자기 개발 머신에서 처음 돌릴 때는 그 사실을 알고 돌린다.

## 밀면 CI가 돌린다

`main`에 밀거나 PR을 열면 GitHub Actions가 단위 시험과 통합 시험과 의존성 취약점 검사를 돌린다. 통합 시험은 러너에서 네임스페이스를 만들고 csa를 실제로 띄운다. 취약점 검사는 `govulncheck`가 한다. 목록을 돌 때마다 새로 받아 오므로 어제 통과한 것이 오늘 걸릴 수 있다.

쓰는 action은 커밋 SHA로 고정한다. 태그는 옮길 수 있어 같은 태그가 다른 코드를 가리킬 수 있다.

VM 시험은 CI에 없다. libvirt로 실제 VM을 띄우는데 러너 안에서는 중첩 가상화가 되지 않는다. 리졸버를 건드렸다면 손으로 돌린다.

## 코딩 관례

일반적인 Go 관례를 따른다. 아래는 이 리포에서 따로 지키는 것이다.

**`gofmt`로 포맷한다.** `make test`가 `go vet`을 돌리므로 PR 전에 통과해야 한다.

**주석은 무엇이 아니라 왜를 적는다.** 무엇을 하는지는 코드가 말한다. 주석은 왜 이렇게 했고 다른 방법이 왜 틀린지를 적는다. 주석은 한국어로 쓴다.

**화면에 찍는 문구는 한국어로 쓴다.** 이 도구를 운영자가 직접 읽는다. 문구에서 변수 뒤에 조사를 붙이지 않는다. 조사는 앞말의 받침에 따라 달라지는데 변수 값은 미리 알 수 없다. 「%s가 없다」가 아니라 「없다: %s」로 적는다.

**어긋난 곳을 모두 모아 돌려준다.** 설정 검사는 첫 문제를 찾고 멈추지 않는다. 운영자가 한 번에 고칠 수 있게 하려는 것이다.

**커널과 닿는 것과 순수한 판단을 나눈다.** 정책 판단과 설정 검사는 커널 없이 시험할 수 있게 둔다. TUN 인터페이스와 netlink와 nftables를 만지는 코드는 그 자리에 모은다.

**시험은 동작만이 아니라 왜 그 규칙인지를 못박는다.** 결함을 고치며 시험을 더할 때는 그 결함의 본질을 주석으로 적는다.

## 문서를 쓰는 규칙

이 리포의 문서는 기술 문서다. 아래를 어기면 문서로서 쓸모가 없다.

**주어와 목적어를 적는다.** 이 문서에는 행위자가 여럿이다. 앱, csa, 커널, wg, 운영자다. 누가 무엇을 하는지 문장마다 적는다. 「검증에 실패하면 버린다」가 아니라 「csa는 출발지 검사에 실패한 패킷을 버린다」로 적는다.

**정확함이 미려함보다 앞선다.** 읽는 사람이 오해하지 않는 것이 목적이다. 문장이 아름다운지는 따지지 않는다.

**같은 것은 늘 같은 낱말로 부른다.** 「접속 주소」를 어느 줄에서 「엔드포인트」로 바꿔 쓰지 않는다. 반복이 지루해도 그대로 둔다.

**사람이 아닌 것에는 「에」를 쓴다.** 앱과 csa와 커널은 이 문서가 행위자로 다루지만 사람이 아니다. 「앱에게 넘긴다」가 아니라 「앱에 넘긴다」로 적는다.

**비유를 쓰지 않는다.** 「봉투」가 아니라 「암호화한 패킷의 바깥 헤더」라고 적는다.

**한 문장에 판단 하나만 담는다.** 연결어미로 길게 잇지 않는다.

**압축한 명사구를 쓰지 않는다.** 명사를 겹쳐 쌓지 말고 동사가 있는 문장으로 푼다.

**처음 나오는 기술 용어는 그 자리에서 한 문장으로 설명한다.** 뒤에서 설명하겠다고 미루지 않는다.

문서 본문은 한다체로 쓴다. 스크립트가 화면에 찍는 문구는 합니다체로 쓴다. 제목과 표의 열 이름은 명사로 끝낸다.

**시험이 찍는 검사 이름은 한다체로 적는다.** 그것은 화면에 찍는 문구이기 전에 무엇을 확인했는지 적은 문장이고, [TESTING.md](TESTING.md)의 검사 목록과 같은 문장이기 때문이다. csa 자신이 찍는 문구는 그대로 합니다체다.

설계 문서는 `design/` 하나다. 지금 정해져 있는 것만 적는다. 무엇을 버렸고 왜 버렸는지 같은 과정 기록은 남기지 않는다.

## 커밋과 PR

커밋 제목은 한국어 한 줄로 무엇을 했는지 적는다. 본문에는 왜 그렇게 했는지를 적는다.

**큰 변경은 PR보다 이슈가 먼저다.** 설정 파일의 모양, 정책의 뜻, csa가 커널에 무엇을 넣는지가 걸리는 변경은 설계 합의가 먼저 있어야 한다. 코드를 다 쓴 뒤에 방향이 갈리면 서로 손해다.

PR에는 무엇을 어떻게 확인했는지 적는다. 커널과 닿는 것을 고쳤으면 통합 시험을 돌린 결과를 함께 붙인다.

## 이슈

**버그와 질문과 제안은 이슈로 연다.** 비공개로 알려야 하는 것은 고치기 전에 알려지면 쓰는 쪽이 공격당하는 것뿐이고, 그 경로는 [SECURITY.md](SECURITY.md)에 있다.

버그를 적을 때 함께 주면 재현이 빠르다.

- 무엇을 기대했고 무엇이 나왔나
- 실행한 명령과 출력. 키와 주소 같은 값은 지우고 붙인다
- 배포판과 커널 판(`uname -r`), Go 판
- 리졸버가 걸린 문제면 그 머신이 systemd-resolved를 쓰는지 아닌지
- 설정 파일 셋. 개인키는 빼고 붙인다

## 기여와 라이선스

이 리포는 [Apache License 2.0](LICENSE)이다. 보내는 기여도 같은 라이선스로 들어온다. Apache License 2.0의 5절이 그렇게 정하고 있으므로 따로 서명할 문서는 없다.

남의 코드를 가져왔다면 어디서 가져왔고 그 라이선스가 무엇인지 PR에 적는다.

## 행동 강령

[CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)를 따른다.
