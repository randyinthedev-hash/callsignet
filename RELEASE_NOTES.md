# 릴리스 노트: Callsignet

버전마다 무엇을 만들었고 무엇을 고쳤는지 적는다. 최신 버전을 맨 위에 둔다. 발행한 절은 고치지 않는다. 뒤에 잘못이 드러나면 그것을 고친 버전의 절에서 어느 버전부터였는지 밝힌다.

| 버전 | 발행 | csa의 판 |
|---|---|---|
| v0.1.4 | 아직 발행하지 않았다 | `0.1.4` |
| v0.1.3 | 2026-09-11 | `0.1.3` |
| v0.1.2 | 2026-09-10 | `0.1.2` |
| v0.1.1 | 2026-09-10 | `0.1.1` |
| v0.1.0 | 2026-09-10 | `0.1.0` |

---

## v0.1.4 — 아직 발행하지 않았다

csa를 설치하고 올리고 되돌리는 절차를 갖춘 판이다. csa가 하는 일은 바뀌지 않았다.

### 갖춘 것

**systemd 서비스 파일이 있다.** `csa.service`가 묶음에 들어 있다. csa는 root로 돌되 손댈 자리를 좁힌다. `/usr`와 `/boot`는 읽기 전용이고, 홈은 보이지 않고, 장치는 `/dev/net/tun`만 보인다. 설정이 없으면 뜨지 않고, 반쯤 걸린 상태로 멈추면 다시 띄운다.

**설치와 올리기와 되돌리기의 절차가 있다.** [INSTALL.md](INSTALL.md)에 적었고 묶음 안의 `install.sh`가 그것을 밟는다. 판마다 `/opt/callsignet/versions/<판>/`에 두고 링크 하나로 어느 판이 도는지 정한다. 설치된 파일의 임자는 묶음을 누가 풀었든 root다. 올리기와 되돌리기는 같은 길이다. 옮겨 갈 판의 csa로 지금 설정을 먼저 검사하고, 다시 띄운 뒤 15초 안에 답하지 않으면 시도하기 전 그대로 돌아온다. `systemctl restart` 자체가 실패해도, 링크를 옮긴 뒤 서비스 파일을 쓰지 못해도 그렇다. 돌아오지도 못하면 돌아왔다고 하지 않고 반쯤 옮겨진 상태라고 말한다. 판 이름으로 쓸 수 없는 값은 아무것도 바꾸지 않고 거절한다. 처음 설치가 중간에 실패하면 만든 것을 치우고 다시 설치할 수 있게 한다. 다시 띄우기 전에 `systemctl reset-failed`로 앞 판이 되풀이해 죽어 걸린 기동 횟수 제한을 푼다. 풀지 않으면 좋은 판으로 돌아와도 뜨지 못한다. 사전 검사에 걸리면 아무것도 바꾸지 않고, 같은 묶음으로 다시 시도할 수 있다.

**설치 묶음으로 붙인다.** 실행 파일만 따로 붙이지 않는다. 묶음 안에 실행 파일과 서비스 파일과 설치 스크립트와 이 절차와 라이선스가 함께 들어 있다. 실행 파일만 가져가면 그 사본에 라이선스가 없기 때문이다. 체크섬은 묶음에 대해 만든다. `make dist`가 태그 워크플로와 같은 스크립트로 같은 모양을 만든다.

### 확인한 것

단위 시험과 통합 시험과 의존성 취약점 검사는 CI가 돌린다. 설치 절차는 깨끗한 Ubuntu 24.04와 Rocky 9에서 문서대로 설치하고 띄우고 다음 판으로 올렸다가 앞 판으로 되돌리는 시험으로 본다. 머신마다 스물여섯이다. 이 시험이 잡은 것이 둘이고 그 기록도 [results/](results/)에 그대로 있다. 첫 실행에서 Rocky가 SELinux에 막혔다. 그 뒤 Ubuntu에서, 나쁜 판이 되풀이해 죽은 직후에 좋은 판으로 돌아와도 systemd의 기동 횟수 제한에 걸려 뜨지 못했다. VM 시험은 `0f16268`에서 돌려 열여덟 가지를 확인했다. csa의 코드와 서비스 파일은 그 뒤로 바뀌지 않았다. 설치 스크립트는 그 뒤에 바뀌었고 설치 시험을 다시 돌린다.

### 아직 없는 것

바이너리 서명과 출처 증명이 없다. 부품 목록(SBOM)이 없다. csa가 권한을 내려놓지 않고 root로 돈다.

---

## v0.1.3 — 2026-09-11

v0.1.2가 만든 회귀 하나를 고친 판이다. **v0.1.2를 쓰고 있고 `psk.dir`를 적어 두었다면 이 판으로 올리기를 권한다.**

### 고친 것

**사전 공유키를 둘 자리가 아직 없어도 csa가 뜬다.** v0.1.2는 비밀 파일을 읽기 전에 그 파일이 놓인 자리를 먼저 보았다. 그래서 파일이 없는 것과 그 파일이 놓일 디렉터리가 아직 없는 것을 가리지 못했다. `psk.mode`가 `optional`인데 `psk.dir`에 적은 디렉터리를 아직 만들지 않았으면, csa는 「사전 공유키 파일이 놓인 자리를 볼 수 없다」고 알리고 뜨지 않았다. v0.1.1까지는 그 자리에서 키가 없는 것으로 보고 그대로 떴다.

이제 파일을 먼저 열고 그다음에 그 자리를 본다. 여는 것만으로는 내용을 읽지 않으므로 순서를 바꾸어도 안전하다. 자리가 안전하지 않으면 내용을 읽기 전에 거절하는 것은 그대로다.

**이 회귀는 v0.1.2에서 들어갔다.** 비밀 파일이 놓인 자리를 보는 검사를 그 판에서 처음 넣었다. 그 판의 시험은 모두 디렉터리를 먼저 만들고 돌았기 때문에 이 자리가 드러나지 않았다.

### 확인한 것

단위 시험과 통합 시험과 의존성 취약점 검사는 CI가 돌렸다. `psk.dir`에 적은 자리가 아직 없을 때 `LoadPSK`가 그것을 키가 없는 것으로 보는지 단위 시험을 하나 더했다. VM 시험도 이 판의 실행 코드가 마지막으로 바뀐 커밋 `5fd2c77`에서 다시 돌려 열여덟 가지를 확인했다. 기록은 [results/](results/)에 있다.

### 아직 없는 것

v0.1.2와 같다. 바이너리 서명과 출처 증명이 없다. 부품 목록(SBOM)이 없다. systemd 서비스 파일이 없다. 설치와 올리기와 되돌리기의 절차가 없다.

---

## v0.1.2 — 2026-09-10

밖에서 온 검토가 짚은 것을 고치고, 붙인 바이너리를 받는 사람이 스스로 잴 수 있게 한 판이다. 새 기능은 없다.

### 고친 것

**비밀 파일을 한 번만 읽는다.** 앞서는 개인키를 세 번 읽었다. 설정 검사가 파일이 안전한지 보면서 한 번, 공개키 짝을 확인하면서 한 번, csa가 기동하면서 wg에 넣으려고 한 번이다. 그 사이에 파일이 바뀌면 **짝을 확인한 키와 실제로 쓰는 키가 다른 것이 된다.** 이제 csa는 개인키와 사전 공유키를 한 번씩 읽어 들고 있고, 검사도 wg 설정도 그 바이트를 쓴다.

**비밀 파일을 여는 방법을 정했다.** 파일을 한 번 열고, 그 파일 설명자에서 형식과 임자와 권한을 보고, 같은 설명자로 내용을 읽는다. 경로로 검사한 뒤에 경로로 다시 여는 것은 안전하지 않다. 그 사이에 남이 파일을 갈아 끼울 수 있다. 경로의 마지막 조각이 심볼릭 링크면 열지 않는다.

**비밀 파일이 놓인 자리도 본다.** 마지막 조각을 막는 것만으로는 모자란다. `/etc/cs/private.key`에서 `cs`가 심볼릭 링크면 csa는 그것을 따라간다. 이제 적은 그대로의 자리와 링크를 모두 푼 자리를 뿌리까지 훑어, 다른 사용자가 고칠 수 있는 디렉터리가 하나라도 있으면 거절한다. 끈적임 비트가 선 디렉터리는 거절하지 않는다. 그런 자리는 누구나 쓸 수 있어도 남의 항목을 건드리지 못한다. **디렉터리의 임자는 보지 않는다.** csa를 root로 돌리면서 키를 운영자의 홈 아래에 두는 것이 흔하고, 그것을 막으면 갈 곳이 없어진다. 이 한계는 설계 문서에 적었다.

**사전 공유키 자리에 FIFO를 두어도 csa가 멈추지 않는다.** 앞서는 `checkPSK`가 내용을 먼저 읽고 나서 일반 파일인지 보았다. FIFO를 읽기로 열면 쓰는 쪽이 붙을 때까지 여는 자리에서 멈춘다. 그래서 검사에 닿기 전에 `csa check`나 기동이 멈출 수 있었다. 이제 멈추지 않게 열고 일반 파일이 아니라고 적는다.

**사전 공유키를 갈아 끼우면 csa reload가 그것을 실제로 건다.** 사전 공유키는 TOML이 아니라 파일에 있다. 앞서 csa는 `csa.toml`과 `peers.toml`과 `policy.toml`만 견주었다. 그래서 운영자가 두 머신에서 키를 바꾸고 `csa reload`를 해도 csa는 「바뀐 것이 없습니다」라고 답하고 **옛 키로 계속 돌았다.** 운영자는 새 키가 걸렸다고 여긴다. 이제 두 설정이 읽어 둔 키를 견주어 바뀐 상대를 찾고, 그 상대를 wg에 다시 건다. 보고에도 어느 상대의 키가 바뀌었는지 적는다. 키 자체는 담지 않는다. 담으면 보고와 로그로 새어 나간다.

**`off`인데 `nft`를 찾지 못하면 확인하지 못했다고 알린다.** 앞서는 `nft`가 없는 것을 표도 없는 것으로 보아 성공으로 처리했다. 커널에 걸린 nftables 표는 `nft` 실행 파일을 지워도 남는다. 그래서 csa가 「포트가 터널 밖에서도 열려 있다」고 알리는 동안 옛 표가 계속 그 포트를 막을 수 있었다. 이제 csa는 뜨되 남은 표를 보지 못했다고 적고 `nftables`를 설치하라고 이른다.

**보지 못했다는 것을 `csa status`도 알린다.** 기동 로그에만 두면 그때 화면을 본 사람만 안다. 뒤에 상태를 묻는 사람은 「닫지 않음」과 「막은 패킷 0개」만 보고 포트가 열려 있다고 여긴다. 상태에 `guard-unchecked`를 두고 화면에도 그 줄을 적는다.

**남은 표를 지울 때 오류 문구를 읽지 않는다.** 앞서는 「그런 표가 없다」는 `nft`의 답을 문구로 가렸다. 그 문구는 배포판과 로캘마다 다르다. 이제 표를 만들고 지우는 배치를 쓴다. 표가 없어도 `nft`가 잘못이라고 답하지 않는다.

**심볼 표를 지우지 않고 만든다.** 심볼 표는 바이너리 안에 어떤 함수가 들어 있는지 적어 둔 표다. v0.1.1까지는 `-ldflags "-s -w"`로 만들었고 `-s`가 그 표를 지웠다. 표가 없으면 `govulncheck`는 바이너리가 어느 함수를 부르는지 보지 못하고 모듈 단위로만 본다. **v0.1.1의 바이너리를 `govulncheck -mode=binary`로 재면 취약점 서른 가지가 나온다. csa는 그 서른 가지의 함수를 하나도 부르지 않는다.** 같은 커밋을 `-s` 없이 다시 만들어 재면 「취약점 없음」이 나온다. `golang.org/x/crypto/ssh`와 `golang.org/x/net/html`처럼 그 서른 가지가 사는 패키지는 v0.1.1의 바이너리 안에 들어 있지도 않다. 이 판은 `-w`만 써서 디버그 정보만 지운다. 바이너리는 7.5MB에서 8.1MB로 커진다.

**가져다 쓰는 것의 판을 올린다.** `golang.org/x/crypto`를 v0.37.0에서 v0.57.0으로, `golang.org/x/net`을 v0.39.0에서 v0.59.0으로, `golang.org/x/sys`를 v0.32.0에서 v0.48.0으로 올렸다. csa가 부르지 않는 자리의 취약점이라도 목록에 남으면 받는 사람이 하나씩 가려야 한다. `govulncheck`가 세던 「요구하는 모듈 안의 취약점」이 서른에서 하나로 줄었다. 남은 하나는 `golang.org/x/crypto/openpgp`가 관리되지 않는다는 것이고 고친 판이 없다. csa는 그 패키지를 바이너리에 넣지 않는다.

**설정 검사를 셋 더했다.** 비밀 파일 자리가 심볼릭 링크인 것, 그 파일이 너무 큰 것, 그 파일이 놓인 자리를 다른 사용자가 고칠 수 있는 것이다.

### 갖춘 것

**태그를 붙이는 워크플로가 붙일 바이너리를 그대로 잰다.** 앞서는 소스만 쟀다. 소스를 잰 것과 붙이는 것이 같다는 보장이 없다. v0.1.1에서 어긋난 자리가 바로 여기다.

### 정정

**설정 검사의 수를 세는 방법을 목록에 맞췄다.** [TESTING.md](TESTING.md)는 검사를 한 문장씩 늘어놓고 그 수를 함께 적는다. 그런데 세는 방법이 목록과 달라, 한 문장이 담은 것을 따로 세기도 하고 그러지 않기도 했다. v0.1.1까지 적힌 「쉰한 가지」가 그렇게 나온 수다. 이제 목록의 문장 하나가 검사 하나다. 그렇게 세면 마흔일곱 가지다. 검사가 줄어든 것이 아니라 세는 방법이 달라진 것이고, 이 판은 오히려 셋을 더했다.

**「아직 없는 것」을 판마다 직접 늘어놓는다.** v0.1.1 절은 그것을 「v0.1.0과 같다」고 적었고, v0.1.0 절은 의존성 취약점 검사가 없다고 적었다. 그런데 그 검사는 v0.1.1에서 갖췄다. 앞 판을 가리키기만 하면 이미 갖춘 것이 계속 없는 것으로 남는다. 이 판부터 그 절에 지금 없는 것을 그대로 적는다.

**VM 시험을 어느 커밋에서 돌리는지 정확히 적었다.** [TESTING.md](TESTING.md)는 「발행할 커밋」에서 돌린다고 적었는데, 실제로는 발행할 실행 코드가 마지막으로 바뀐 커밋에서 돌린다. 그 뒤에 기록과 문서만 바뀌었다면 다시 돌리지 않는다. 무엇이 실행 코드인지도 함께 적었다.

### 확인한 것

단위 시험과 통합 시험과 의존성 취약점 검사는 CI가 돌렸다. 가져다 쓰는 것의 판을 올렸고 비밀 파일과 `guard`와 설정을 다시 읽는 길의 코드를 고쳤으므로 바이너리에 들어가는 코드가 바뀌었다. 그래서 VM 시험도 이 판의 실행 코드가 마지막으로 바뀐 커밋 `0f64d7a`에서 다시 돌려 열여덟 가지를 확인했다. 기록은 [results/](results/)에 있다.

### 아직 없는 것

바이너리 서명과 출처 증명이 없다. 부품 목록(SBOM)이 없다. systemd 서비스 파일이 없다. 설치와 올리기와 되돌리기의 절차가 없다. 기업 내부에서 실제로 돌리려면 그것이 있어야 한다. 그것을 갖추기 전까지 이 판으로 운영 배포를 하지 않기를 권한다.

---

## v0.1.1 — 2026-09-10

밖에서 온 검토가 짚은 것을 고친 판이다. 새 기능은 없다.

### 고친 것

**설정 파일에 모르는 열쇠가 있으면 거절한다.** `[psk]`의 `mode`를 `modee`로 잘못 적으면 `mode`가 빈 값이 되고, 빈 값은 `optional`과 같았다. 운영자는 사전 공유키를 반드시 쓰게 했다고 여기는데 csa는 키 없이 기동했다. 오타 하나가 조용히 보안을 낮추는 자리였다.

**설정을 걸다 실패하고 되돌리지도 못하면 csa가 멈춘다.** 앞서는 알리기만 하고 계속 돌았다. 어느 판이 걸려 있는지 알 수 없는 채로 도는 것이 문제다. 새 정책이 권한을 넓히는 것이었다면 그것이 걸린 채로 돈다. csa는 터널을 닫되 직통 경로 규칙은 남기고 멈춘다. 지우면 서비스 포트가 터널 밖으로 다시 열린다.

**`guard.mode`를 `off`로 띄우면 앞서 남은 규칙을 지운다.** csa는 반쯤 걸린 상태로 멈출 때 직통 경로 규칙을 일부러 남기고, `SIGKILL`로 죽어도 남는다. 그것을 풀려고 운영자가 `off`로 바꿔 다시 띄우면, 새 csa는 자기가 건 것이 없다고 여겨 아무것도 지우지 않았다. csa는 「닫지 않는다」고 알리는데 실제로는 옛 표가 계속 포트를 막았다. 이제 자기가 무엇을 걸었는지와 무관하게 커널에 있는 표를 찾아 지운다.

**개인키와 사전 공유키 파일이 안전한 자리에 있는지 본다.** 앞서는 있는지만 보아서 디렉터리도 지나고 `0644`인 개인키도 지났다. 일반 파일인지, 읽을 수 있는지, 다른 사용자에게 권한이 없는지 본다.

**키를 바꿀 때 옆에 온전히 써 두고 한 번에 옮긴다.** 앞서는 있던 파일을 먼저 비워, 쓰다 실패하면 쓰던 정상 키를 잃었다.

**설정 검사를 아홉 더했다.** v0.1.0에 없던 것만 적는다. 15글자를 넘거나 쓸 수 없는 글자를 담은 `tun.name` 둘, 포트가 0인 접속 주소, 전부 0인 공개키, 다른 사용자가 읽을 수 있거나 일반 파일이 아닌 비밀 파일 둘, 설정 파일 셋에 든 모르는 열쇠 셋이다. 모두 검사를 지나 기동이나 handshake에서 실패하거나 보안을 낮추던 것이다.

**표준 라이브러리 취약점 넷을 피한다.** `govulncheck`가 Go 1.26.3에서 `crypto/tls` 둘, `encoding/asn1` 하나, `crypto/x509` 하나를 찾았다. 도구 사슬을 1.26.6으로 올렸다. **v0.1.0의 바이너리에는 이 넷이 들어 있다.**

### 갖춘 것

밀 때마다 의존성 취약점을 검사한다. 태그를 붙이는 워크플로도 같은 검사를 돌린다. 태그를 미는 길로만 오면 검사를 건너뛰는 자리가 있어서는 안 된다. GitHub Actions를 커밋 SHA로 고정한다. 태그를 붙이는 워크플로가 바이너리를 만들기 전에 시험을 다시 돌린다. 정적 바이너리에 들어가는 라이브러리의 라이선스를 [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md)에 고지하고 릴리스에 함께 붙인다.

### 정정

v0.1.0 절을 발행 뒤에 두 번 고쳤다. 이 문서가 발행한 절은 고치지 않는다고 정해 두었는데 그것을 어겼다. **v0.1.0 절을 태그의 것과 똑같이 되돌렸고, 그 절의 잘못은 아래에 적는다.**

v0.1.0 절은 「네 계층으로 나누어 확인한다」고 적었다. 그 판에서 실제로 돌린 것은 단위 시험과 통합 시험과 VM 시험 셋이고 관측 실험은 돌리지 않았다. 계층을 설명한 문장이 그 판에서 무엇을 돌렸다는 말로 읽힌다.

v0.1.0의 바이너리에는 표준 라이브러리 취약점 넷이 들어 있다. 그 판을 낼 때는 의존성 취약점 검사가 없었다. v0.1.0을 쓰고 있다면 이 판으로 올리기를 권한다.

### 확인한 것

네 계층을 모두 돌렸다. 단위 시험과 통합 시험과 의존성 취약점 검사는 CI가 밀 때마다 돌린다. VM 시험은 Ubuntu와 Rocky에서 손으로 돌려 열여덟 가지를 확인했다. `off`를 고친 커밋 `009b851`에서 돌렸다. 관측 실험도 돌렸다. 두 기록 모두 [results/](results/)에 있다.

관측 실험의 기록에는 어느 커밋에서 돌렸는지 적혀 있지 않다. 기록을 만드는 스크립트가 적지 않았다. 그것을 고쳤으므로 다음에 돌리는 것부터 적는다. 그 기록을 만든 뒤에 `poc/cryptokey/`에서 바뀐 것은 만든 파일의 주인을 부른 사람에게 넘기는 것뿐이고, 실험이 재는 것은 그대로다.

### 아직 없는 것

v0.1.0과 같다. 여기에 더해 바이너리 서명과 출처 증명, 부품 목록(SBOM), systemd 서비스 파일과 설치·업그레이드·되돌리기 절차가 없다. 기업 내부에서 실제로 돌리려면 그것이 있어야 한다.

---

## v0.1.0 — 2026-09-10

머신 둘이 이름으로 서로를 부르고 연결이 맺어지는 순간에 상대를 확정하는 데까지다.

**이 판은 기술 프리뷰다.** 통제된 리눅스 환경에서 시험 운용을 검토할 수 있는 수준이다. 기업 내부에서 실제로 돌리려면 아직 없는 것이 있다. 의존성 취약점 검사, 바이너리 서명, 부품 목록(SBOM), systemd 서비스 파일, 설치와 올리기와 되돌리기의 절차다. 그것을 갖추기 전까지 이 판으로 운영 배포를 하지 않기를 권한다.

### 만든 것

csa가 머신마다 하나 돌며 터널을 운영한다. 상대의 정적 공개키로 세션을 맺고, 사전 공유키를 함께 섞는다. 접속 주소가 바뀌어도 인증을 통과한 패킷에서 새 주소를 배운다.

csa가 내부 도메인의 이름 해석기가 된다. 리졸버 자리를 파일을 고쳐 차지하거나 systemd-resolved에 등록해 차지하고, 멈추면서 되돌린다. 자기 도메인이 아닌 이름은 거절해 그 머신의 다른 이름 해석을 가로채지 않는다.

csa가 받는 쪽에서 정책을 집행한다. 어느 상대의 어느 app이 이 머신의 어느 포트에 붙어도 되는지 `policy.toml`이 정한다. IP 대역을 조건으로 둘 수 있고 그때는 만료 기한을 함께 적는다. 막은 연결은 앱에 거절 응답으로 알린다.

csa가 직통 경로를 닫는다. 터널을 거치지 않고 이 머신의 실제 IP로 서비스 포트에 붙는 것을 nftables로 막는다. 서비스 포트만 닫는 것과 예외 말고 모두 닫는 것 가운데 고른다. 멈추면서 규칙을 지운다.

csa가 도는 중에 설정을 다시 읽는다. 정책이 바뀌면 그 뒤에 열리는 연결만이 아니라 **이미 들여 둔 연결도 다시 재어** 허가되지 않는 것을 잊는다. 다시 읽을 수 없는 것이 바뀌면 아무것도 걸지 않고 다시 띄우라고 알린다.

`csa status`가 상대마다 관측한 출발지와 마지막 handshake 시각과 주고받은 바이트를 보여 준다.

### 확인한 것

단위 시험과 통합 시험과 관측 실험과 VM 시험, 네 계층으로 나누어 확인한다. 통합 시험은 네임스페이스로 머신 셋을 만들어 csa 둘을 실제로 띄우고 일흔한 가지를 본다. VM 시험은 Ubuntu와 Rocky를 실제로 띄워 네임스페이스가 흉내 내지 못하는 리졸버 갈래를 본다. 무엇을 어떻게 확인하는지는 [TESTING.md](TESTING.md)에 있다.

### 아직 없는 것

IPv6 접속 주소는 설정 검사가 받아들이지만 실제로 확인하지 않았다. 터널 IP는 IPv4만 된다.

조각난 패킷의 뒷조각은 포트로 판단하지 못하고 상대 단위로만 판단한다.

중계는 없다. 두 머신이 서로 닿을 수 있어야 한다.
