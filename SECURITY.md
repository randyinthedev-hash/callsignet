# 보안 정책

Callsignet은 머신과 서비스가 서로를 알아보고 인증하는 일을 네트워크 계층에서 맡는다. csa는 정적 개인키와 사전 공유키를 다루고, 어느 머신이 어느 머신에 붙어도 되는지를 집행하고, 그 머신의 이름 해석 자리와 방화벽 규칙을 고친다. 그래서 보안 신고를 다른 일보다 앞에 둔다.

## 지원하는 판

보안 수정은 `main`과 최신 릴리스에 넣는다. 지난 태그로는 백포트하지 않는다. 한 사람이 만드는 리포라 여러 갈래를 유지할 수 없다. 유지하는 척하면 쓰는 쪽이 오지 않은 수정을 왔다고 여기게 된다.

| 판 | 보안 수정 |
|---|---|
| `main` | 넣는다 |
| 최신 릴리스 | 넣는다. 다음 릴리스로 나간다. 오래 둘 수 없는 결함이면 패치 릴리스를 따로 낸다 |
| 그 이전 태그 | 넣지 않는다. 최신 릴리스로 올린다 |

## 취약점 신고

**공개 이슈나 PR이나 토론으로 올리지 않는다.** 고치기 전에 알려지면 쓰는 쪽이 그동안 공격당한다.

비공개 경로 둘 가운데 하나로 알린다.

1. **GitHub 비공개 신고.** 이 리포의 **Security 탭 → Report a vulnerability**로 연다. 신고와 오가는 말이 리포에 비공개로 남고, 고친 뒤 함께 공개할 수 있다.
2. 위가 어려우면 **메인테이너에게 직접** 알린다. 주소는 <randyinthedev@gmail.com>이다. 제목에 `[security]`를 달면 빨리 눈에 띈다.

신고에 다음을 담아 주면 분류가 빠르다.

- 어느 부분인가. 터널, 이름 해석, 정책 집행, 직통 경로 닫기, 키 다루기 가운데 무엇인가
- 재현하는 절차
- 무엇이 무너지는가. 정책 우회, 신원 확인 우회, 키 노출, 이름 해석 가로채기 가운데 무엇인가
- 가능하면 재현 코드. 이 리포의 하네스(`poc/tunnel/run.sh`)를 고쳐 만든 것이면 가장 좋다

**취약점이 아닌 것은 공개 이슈로 여는 것이 맞다.** 버그와 질문과 제안은 이 리포의 이슈로 연다. 숨길 까닭이 없고 공개된 논의가 다음 사람에게 남는다. 비공개로 알려야 하는 것은 고치기 전에 알려지면 쓰는 쪽이 공격당하는 것뿐이다.

## 무엇을 취약점으로 보나

Callsignet이 하겠다고 말한 것이 무너지면 취약점이다. 그 주장은 [design/README.md](design/README.md)에 있다.

- **정책 우회.** 받는 쪽 csa가 허가하지 않은 상대의 패킷을 앱에 넘기는 것이다. `policy.toml`의 `[[inbound]]`에 적은 `allow`와 `allow-cidr`와 `expires`가 그 판단의 근거다.
- **신원 확인 우회.** 상대의 정적 개인키를 쥐지 않은 쪽과 세션이 서는 것이다. 한 상대의 자리에 다른 상대가 들어서는 것도 여기에 든다.
- **직통 경로가 열리는 것.** 터널을 거치지 않은 연결이 앱에 닿는 것이다. 직통 경로를 닫는 부분이 열어 두어서는 안 될 포트를 열거나, 그 머신의 다른 방화벽 규칙을 지우는 것도 여기에 든다.
- **철회가 듣지 않는 것.** 정책에서 빠진 상대와 이미 오가던 연결이 `csa reload` 뒤에도 이어지는 것이다.
- **키가 새는 것.** 정적 개인키나 사전 공유키가 로그나 `csa status`의 출력이나 파일 권한을 통해 새는 것이다.
- **이름 해석을 가로채는 것.** csa가 리졸버 자리를 차지하는 동안 자기 도메인 밖의 이름에 잘못 답해, 그 머신의 다른 이름 해석을 가로채거나 망가뜨리는 것이다.
- **되돌리지 못하는 것.** csa가 고친 리졸버 설정과 방화벽 규칙을 멈출 때 되돌리지 못해, 그 머신이 이름을 해석하지 못하거나 열려 있어야 할 포트가 막힌 채로 남는 것이다.

## 무엇이 범위 밖인가

- **운영자가 적은 설정 자체.** `allow`에 모두를 적어 두었거나 `guard.mode = "off"`로 두어 생기는 일은 취약점이 아니다. csa는 운영자가 적은 대로 집행한다.
- **앱이 하는 일.** csa는 어느 머신이 어느 포트에 붙어도 되는지까지 판단한다. 연결이 선 뒤에 앱이 무엇을 하는지는 앱의 몫이다.
- **가져다 쓰는 라이브러리 자체의 결함.** wg 구현(`golang.zx2c4.com/wireguard`)이나 다른 의존의 결함은 그쪽에 알린다. 다만 csa가 그것을 잘못 쓰고 있는 것이면 이 리포의 문제다.
- **그 머신의 root 권한을 이미 쥔 공격자.** csa는 자기가 도는 머신 안에서 root가 하는 일을 막지 못한다. 설정 파일과 키 파일을 고치는 것, csa를 멈추는 것, nftables 규칙을 지우는 것이 모두 여기에 든다.

## 응대

한 사람이 만드는 리포라 되는 만큼 응대한다. 접수를 확인하고, 재현하고, 무엇을 어떻게 고칠지 신고자와 맞춘다. 기한을 약속하지는 않는다. 다만 유효한 신고는 다른 작업보다 앞에 둔다.
