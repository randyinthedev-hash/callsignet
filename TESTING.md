# 시험

무엇을 어떻게 확인하는지 적는다. 시험을 새로 쓸 때 어느 계층에 넣을지 여기서 정한다.

## 세 계층

시험을 셋으로 나눈다. 각각 답하는 질문이 다르다.

| 계층 | 답하는 질문 | 도구 | root |
|---|---|---|---|
| 단위 시험 | 우리가 정한 규칙대로 판단하는가 | `go test` | 필요 없다 |
| 통합 하네스 | 커널과 닿는 부분이 실제로 도는가 | network namespace | 필요하다 |
| 관측 실험 | 설계가 기댄 성질이 참인가 | network namespace | 필요하다 |

**단위 시험은 커널과 닿지 않는다.** 설정을 읽고 검사하는 규칙, UAPI 설정 문자열을 만드는 규칙처럼 순수한 판단만 확인한다. 빠르고 root가 필요 없으므로 고칠 때마다 돌린다.

**통합 하네스는 csa를 실제로 띄운다.** TUN 인터페이스를 만들고 경로를 넣고 wg 세션을 맺는 일은 커널이 해 주므로 흉내 낼 수 없다. 네임스페이스를 만들어 진짜로 돌려 본다.

**관측 실험은 우리 코드를 확인하지 않는다.** 설계가 기대고 있는 성질이 실제로 참인지 잰다. 회귀 시험이 아니라 근거 확보다. 설계 문서가 주장하는 것을 관측으로 뒷받침한다.

## 시험을 쓰는 방식

**제대로 된 것 하나를 두고 한 군데만 어긋뜨린다.** 설정 검사 시험이 그렇다. 어긋난 곳이 없는 설정을 만드는 함수를 하나 두고, 각 시험은 거기서 한 값만 바꾼 뒤 그 문제가 잡히는지 본다. 어느 검사가 어느 잘못을 잡는지 한눈에 보인다.

**어긋난 곳을 모두 모아 돌려준다.** csa는 첫 문제를 찾고 멈추지 않는다. 운영자가 한 번에 고칠 수 있게 하려는 것이다. 시험도 그 목록에서 기대한 문구를 찾는 방식으로 쓴다.

**화면에 찍는 문구도 시험이 본다.** 실제로 두 가지를 잡았다. 설계 문서의 TOML 예제에서 `outbound`가 표 뒤에 있어 그 표의 항목으로 읽히던 것과, 화면 문구에서 변수 뒤에 조사를 붙여 「없음가」가 되던 것이다.

**하네스는 머신을 실제로 고치므로 되돌리는 것까지 확인한다.** csa가 리졸버 설정을 고치는 부분이 그렇다. 하네스가 `/etc/resolv.conf`를 심볼릭 링크로 두고 돌린 덕에, csa가 링크를 지우고 그 자리에 파일을 만들어 그 머신의 이름 해석 설정을 망가뜨리는 것을 잡았다. 단위 시험은 이것을 잡을 수 없다. `/etc`를 실제로 건드려 보아야 드러난다.

**하네스는 스스로 정리한다.** 네임스페이스와 브리지를 만든 스크립트가 끝날 때 지운다. 실패해도 지운다.

## 지금 있는 시험

### 단위 시험

`make test`로 돌린다.

| 꾸러미 | 확인하는 것 |
|---|---|
| `internal/config` | 설정 셋을 읽는다. 검사 열일곱 가지가 각각 제 잘못을 잡는다 |
| `internal/wgdev` | UAPI 설정 문자열을 만든다. 자기 자신을 상대 목록에서 뺀다. 길이가 틀린 키를 거부한다 |
| `internal/name` | 정방향과 역방향을 답한다. 설정에 없는 이름과 우리 도메인이 아닌 이름을 가른다. 리졸버 관리 방식을 판별한다. 심볼릭 링크를 따라간다 |
| `internal/policy` | 받는 쪽과 나가는 쪽 판단. IPv4 패킷에서 주소와 포트를 읽는다. 거절 응답을 만든다 |

설정 검사 열다섯 가지는 이렇다. 자기 peer-id가 목록에 없다. 터널 IP가 겹친다. 터널 IP가 대역 밖이다. 공개키가 두 peer에 나타난다. peer-id가 두 번 나온다. 개인키 파일이 없다. 접속 주소를 읽을 수 없다. 정책이 없는 peer를 가리킨다. 정책이 없는 app을 가리킨다. outbound 형태가 틀렸다. inbound가 이 머신에 없는 app을 가리킨다. IP 대역 규칙에 만료 기한이 없다. 그 기한이 이미 지났다. 포트가 범위를 벗어났다. MTU가 범위를 벗어났다.

### 통합 하네스

`sudo make tunnel`로 돌린다.

네임스페이스 둘을 브리지로 잇고, 스크립트가 키와 설정 파일 셋을 만들고, 각 네임스페이스에서 `csa run`을 띄운다. 그다음 한쪽 터널 IP에서 다른 쪽 터널 IP로 ping을 보낸다.

첫 통신이 몇 초 늦을 수 있다. 두 csa가 같은 순간에 뜨면 둘 다 handshake를 걸고, 그러면 각자 상대의 응답을 받을 자리를 이미 응답 상태로 덮어써서 첫 시도가 어긋난다. wg가 몇 초 뒤에 다시 걸어 세션이 선다. 하네스는 20초까지 기다린다.

이것이 확인하는 것은 열일곱이다.

터널이 서는 것 넷이다. csa가 설정을 읽고 검사를 통과한다. TUN 인터페이스를 만들고 터널 IP와 경로와 MTU를 건다. 두 csa가 wg 세션을 맺는다. 앱이 터널 IP로 통신한다.

csa가 리졸버 자리를 차지하는 것 넷이다. csa가 `/etc/resolv.conf` 첫 줄에 자기 주소를 넣는다. 원래 리졸버를 둘째 줄에 남긴다. csa가 설정을 건 뒤 스스로 질의해 확인한다. csa가 멈추면서 원래 내용으로 되돌린다.

이름이 풀리는 것 셋이다. csa가 서비스 이름과 머신 이름과 역방향을 답한다. 설정에 없는 이름에는 없다고 답하고 우리 도메인이 아닌 이름은 거절한다. 앱이 이름으로 상대를 불러 통신한다.

기록이 남는 것 둘이다. 받아들인 연결의 기록에 peer-id와 관측한 출발지가 함께 남는다. 연결마다 한 번만 적고 패킷마다 적지 않는다.

정책이 집행되는 것 넷이다. 나가는 쪽이 정책에 없는 포트를 막는다. 나가는 쪽이 정책에 없는 상대를 막는다. 받는 쪽이 정책에 없는 연결을 막는다. 허가된 연결은 그대로 지난다.

정책 시험은 일부러 어긋나게 짠 설정으로 한다. `srv-b`에 `secret`이라는 앱을 두고, `srv-a`는 그리로 나가도 되지만 `srv-b`는 들이지 않게 한다. 그러면 나가는 쪽은 통과시키고 받는 쪽이 막으므로, 받는 쪽이 최종 판단 주체라는 것이 관측으로 드러난다. `peers.toml`에만 있고 정책에 없는 `srv-c`도 둔다. 이름은 풀리지만 통신은 막혀야 한다.

리졸버 시험은 네임스페이스가 파일을 갈아 끼우는 성질을 쓴다. `ip netns exec`는 `/etc/netns/<이름>/resolv.conf`가 있으면 그것을 `/etc/resolv.conf` 자리에 붙여 준다. 하네스는 그 파일에 회사 DNS 주소만 적어 두고, csa가 자기 주소를 첫 줄에 넣는지 본다.

여기에 걸리는 것이 하나 있다. `mount`는 대상 경로의 심볼릭 링크를 따라가므로, 파일이 실제로 붙는 자리는 `/etc/resolv.conf`가 아니라 그 링크가 가리키는 곳이다. 네임스페이스 안에서도 `/etc/resolv.conf`는 여전히 링크다. csa가 링크를 따라가지 않으면 엉뚱한 곳을 고치게 되고, 마운트 네임스페이스는 나뉘어 있어도 파일 시스템은 하나이므로 호스트의 파일이 망가진다.

### 관측 실험

`sudo make cryptokey`로 돌린다.

설계는 받는 쪽 csa가 상대를 확정한다고 말하고, 그 근거로 wg의 동작을 든다. wg는 복호화한 패킷의 출발지 IP가 그 상대에게 허용된 목록 안에 없으면 버린다.

이 실험은 그것을 잰다. 허용된 출발지와 허용 목록 밖 출발지로 각각 패킷을 보내고, 받는 쪽 `wg0`의 통계가 어떻게 갈리는지 본다. 허용 목록 밖에서 온 패킷은 `rx_packets`를 올리지 않고 `rx_errors`만 올린다.

기록은 `results/`에 남는다. 설계 문서가 주장하는 것의 근거이므로 지우지 않는다.

## 아직 없는 것

만들면서 함께 채운다.

`csa status`와 `csa reload`가 없다. 만들 때 함께 쓴다.

IP 대역만 보는 정책을 집행하지 않는다. 설정 검사는 하지만 실제로 거르지는 않는다. 그 판단은 관측한 출발지를 기준으로 해야 한다.

systemd-resolved와 NetworkManager 갈래를 하네스가 밟지 않는다. 지금 하네스가 확인하는 것은 `/etc/resolv.conf`를 직접 고치는 갈래뿐이다. 어느 갈래로 갈지 판별하는 규칙은 단위 시험이 본다.

거절 응답이 앱에게 제대로 닿는지 확인하지 않는다. csa가 「관리자가 막았다」는 뜻의 ICMP를 돌려주는데, 앱이 그것을 받아 곧바로 실패하는지는 아직 재지 않는다. 지금은 로그로만 확인한다.

MTU와 조각화를 확인하지 않는다. 큰 패킷이 지나가는지 보아야 한다.

오래 도는 동안의 문제를 확인하지 않는다. keepalive가 실제로 나가는지, 접속 주소가 바뀌었을 때 따라가는지다.

설정을 다시 읽는 동작을 확인하지 않는다. `csa reload`를 만들 때 함께 쓴다.
